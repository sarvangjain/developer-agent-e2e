services:
  qdrant:
    image: qdrant/qdrant:v1.15.0
    container_name: developer-agent-qdrant
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_data:
